{
  "buildCommand": "npm install --legacy-peer-deps --ignore-scripts=false && npm run build",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts=false",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
